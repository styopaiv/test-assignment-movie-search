import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  content: {
    display: "flex",
    justifyContent: "space-around",
    alignContent: "center",
  },
  search: {
    lineHeight: "30px",
  },
});
